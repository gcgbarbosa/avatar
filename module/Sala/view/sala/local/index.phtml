<div class="panel panel-default">
    <?php
    // module/Album/view/album/album/index.phtml:

    $title = 'Setor';
    $this->headTitle($title);
    ?>
    <div class="panel-heading">
        <h1><?php echo $this->escapeHtml($title); ?></h1>
    </div>
    <div class="panel-body">
        <p>
            <a class="btn btn-success" href="<?php echo $this->url('local', array('action'=>'add'));?>"><span class="glyphicon glyphicon-plus"></span> Adicionar Setor</a>
        </p>

        <table class="table">
        <thead>
            <th>Nome</th>
            <th>&nbsp;</th>
        </thead>
        <?php foreach ($locais as $local) : ?>
        <tr>
            <td><?php echo $this->escapeHtml($local->getNomelocal());?></td>
            
            <td>
                

                <a class="btn btn-danger" href="<?php echo $this->url('local',
                    array('action'=>'delete', 'id' => $local->getIdlocal()));?>"><span class="glyphicon glyphicon-remove"></span> Deletar</a>
            </td>
        </tr>
        <?php endforeach; ?>
        </table>
    </div>
</div>

